summary: FDO bootc test plan
description: FDO device onboarding in bootc image

discover:
    how: fmf
    filter: tag:bootc

provision:
    hardware:
        virtualization:
            is-supported: true
        cpu:
            processors: ">= 2"
        memory: ">= 6 GB"

prepare:
    - how: shell
      script: |
        rpm -q --whatprovides 'dnf-command(copr)' &> /dev/null || dnf install -y 'dnf-command(copr)'
        dnf copr list | grep 'fedora-iot/fedora-iot' || {
          . /etc/os-release
          if [ "${ID}" = "centos" ]; then
            dnf copr enable -y '@fedora-iot/fedora-iot' ${ID}-stream-${ID_VERSION}
          else
            dnf copr enable -y '@fedora-iot/fedora-iot'
          fi
        }
        # testing-farm-tag-repository is causing problems with builds see:
        # https://docs.testing-farm.io/Testing%20Farm/0.1/test-environment.html#disabling-tag-repository
        dnf install --disablerepo=testing-farm-tag-repository -y \
          go-fdo-server go-fdo-server-manufacturer go-fdo-server-owner go-fdo-server-rendezvous sqlite
        dnf copr disable -y @fedora-iot/fedora-iot

    - how: install
        package:
        - golang
        - make
        - openssl
        - curl
        - git
        - jq
        - podman
        - qemu-img
        - qemu-kvm
        - libvirt-client
        - libvirt-daemon-kvm
        - virt-install
        - firewalld
        - lorax
        - gobject-introspection

execute:
    how: tmt
